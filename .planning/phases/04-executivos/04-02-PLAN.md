---
phase: 04-executivos
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - public/images/team/
autonomous: false
requirements:
  - EXEC-01

must_haves:
  truths:
    - "Each processed portrait is available as WebP at 400x400 in public/images/team/"
    - "Each portrait weighs under 100KB"
    - "The user has confirmed which filename maps to which executive"
  artifacts:
    - path: "public/images/team/"
      provides: "Processed WebP portraits named by executive slug (e.g. joao-silva.webp)"
  key_links:
    - from: "public/images/team/*.webp"
      to: "src/data/team.ts"
      via: "photo field: '/images/team/joao-silva.webp'"
      pattern: "/images/team/"
---

<objective>
Process source portraits from docs/ into production-ready WebP files. This is the content gate for Phase 4 — Plans 4.3 and 4.4 cannot proceed without completed portraits placed at public/images/team/.

Purpose: Real institutional portraits are the primary trust signal on the Executivos page. Placeholders directly contradict the credibility goal.

Output: Processed WebP portrait files at public/images/team/{slug}.webp
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Identify executives in source photos</name>
  <what-built>
    Source portrait files exist in two directories:
    - docs/imagens clientes com fundo/ — 20 files (image4–image23, mixed jpeg/png)
    - docs/imagens clientes sem fundo/ — 20 files (image4–image23, all png, transparent bg)

    Before processing, the user must identify which file number corresponds to which executive.
  </what-built>
  <how-to-verify>
    1. Open each file in docs/imagens clientes com fundo/ to view the portraits
    2. For each executive in the Kolabore team, note the image number that matches their face
    3. Provide a mapping in this format (one line per executive):
       image4 → Nome Completo
       image5 → Nome Completo
       (etc.)
    4. Also confirm: should we use "com fundo" (with background) or "sem fundo" (transparent/cutout) versions for the cards?
  </how-to-verify>
  <resume-signal>
    Paste the full name-to-filename mapping here, plus which version (com/sem fundo) to use. Example:
    image7 → Carlos Ferreira (sem fundo)
    image12 → Ana Souza (com fundo)
    ...
  </resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Process and export portraits to public/images/team/</name>
  <what-built>
    Once the mapping from Task 1 is confirmed, process each portrait to production spec.
  </what-built>
  <how-to-verify>
    Process each identified portrait file:

    **Processing spec:**
    - Target dimensions: 400×400px minimum (crop to square, keeping face centered)
    - Export format: WebP at 75–80% quality
    - Target file size: under 100KB per portrait
    - Consistent treatment: same brightness/contrast adjustments across all portraits for visual unity
    - Naming convention: kebab-case of full name, e.g. carlos-ferreira.webp

    **Recommended tools:**
    - Squoosh (squoosh.app) — browser-based, WebP export with quality slider, free
    - ImageMagick CLI: `magick input.png -resize 400x400^ -gravity center -extent 400x400 -quality 78 output.webp`
    - Photoshop / Affinity Photo — Export as WebP 75–80%

    **Output location:** Place all processed files in `public/images/team/` (create directory if it doesn't exist)

    **Verification after export:**
    - Confirm all files are in public/images/team/ with .webp extension
    - Confirm each file is under 100KB: `ls -lh public/images/team/`
    - Confirm naming matches executives (e.g. carlos-ferreira.webp, not image7.webp)
  </how-to-verify>
  <resume-signal>
    Type "portraits ready" once all WebP files are in public/images/team/ and each is under 100KB. Provide the complete list of filenames placed (one per line).
  </resume-signal>
</task>

</tasks>

<verification>
- ls public/images/team/ shows only .webp files
- All files are under 100KB (ls -lh confirms)
- Filenames are kebab-case of executive names (no image4.webp etc.)
- File count matches number of executives to be featured on the page
</verification>

<success_criteria>
All executive portraits are processed as WebP at 400×400px, under 100KB each, placed in public/images/team/ with kebab-case executive names. The name-to-filename mapping is documented in the SUMMARY so Plan 4.3 can wire the correct photo paths.
</success_criteria>

<output>
After completion, create `.planning/phases/04-executivos/04-02-SUMMARY.md`

CRITICAL: The SUMMARY must include the complete mapping table:
| Executive Name | Title | Filename |
|----------------|-------|----------|
| Nome Completo  | Sócio | nome-completo.webp |

This table is the input Plan 4.3 needs to populate src/data/team.ts.
</output>
