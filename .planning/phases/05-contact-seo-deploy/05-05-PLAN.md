---
phase: 05-contact-seo-deploy
plan: "05"
type: execute
wave: 3
depends_on:
  - "05-03"
  - "05-04"
files_modified: []
autonomous: false
requirements:
  - CONT-07
  - CONT-08
user_setup:
  - service: formspree
    why: "Contact form delivery to client inbox"
    env_vars:
      - name: NEXT_PUBLIC_FORMSPREE_ID
        source: "Formspree Dashboard → Your Forms → Form ID (e.g. xpwzabcd)"
    dashboard_config:
      - task: "Create a form in Formspree dashboard if not yet done"
        location: "formspree.io → New Form"
      - task: "Add client's email address as notification recipient"
        location: "Formspree Dashboard → Form Settings → Notifications"
      - task: "If client uses Outlook/Microsoft 365: consider configuring custom sending domain"
        location: "Formspree Dashboard → Form Settings → Custom Email (requires Gold/Business plan)"
  - service: hostinger
    why: "Static site hosting — upload out/ to public_html/"
    env_vars: []
    dashboard_config:
      - task: "Access FTP credentials or File Manager"
        location: "hPanel → Hosting → Manage → FTP Accounts or File Manager"
must_haves:
  truths:
    - "All 6 routes load correctly after hard browser refresh on the live Hostinger domain"
    - ".htaccess SPA fallback is present at the domain root — Apache rewrites all requests to index.html"
    - "Form submission from /contato is received in the client's actual inbox (not test account)"
    - "Formspree shows submission received in dashboard"
    - "OG image renders when pasting homepage URL into LinkedIn Post Inspector"
    - "sitemap.xml is accessible at https://[domain]/sitemap.xml"
    - "All 6 page titles are unique (visible in browser tab on each page)"
    - "No console.error on any page in browser DevTools"
  artifacts:
    - path: "out/.htaccess"
      provides: "SPA fallback and HTTPS redirect — copied from public/.htaccess at build"
      contains: "RewriteRule"
  key_links:
    - from: "out/"
      to: "public_html/ on Hostinger"
      via: "FTP upload or File Manager — all out/ contents go into public_html/"
      pattern: "N/A — manual transfer"
    - from: "NEXT_PUBLIC_FORMSPREE_ID"
      to: "ContactSection form submission"
      via: "env var read at build time by @formspree/react"
      pattern: "NEXT_PUBLIC_FORMSPREE_ID"
---

<objective>
Deploy the fully built site to Hostinger and execute the complete pre-launch validation checklist. This plan is entirely human-driven checkpoints — no Claude-automated code changes. All code work is complete in Plans 5.1–5.4.

Purpose: CONT-07 requires all routes working on live Hostinger with hard refresh. CONT-08 requires form deliverability confirmed with the client's real email address. These are the two blocking gates for Phase 5 completion.
Output: Live site on Hostinger, all checklist items passing, Phase 5 complete.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-contact-seo-deploy/05-01-SUMMARY.md
@.planning/phases/05-contact-seo-deploy/05-02-SUMMARY.md
@.planning/phases/05-contact-seo-deploy/05-03-SUMMARY.md
@.planning/phases/05-contact-seo-deploy/05-04-SUMMARY.md

<interfaces>
<!-- Critical deploy context -->

Deployment target: Hostinger shared hosting
Upload: contents of out/ → public_html/ on Hostinger
Domain: https://kolabore.com.br (assumed root — if subdirectory, update metadataBase and sitemap before this plan)

Critical file: out/.htaccess — Apache SPA fallback
This file is copied from public/.htaccess at build time.
It must be present in public_html/ after upload or ALL routes except homepage will 404 on hard refresh.

NEXT_PUBLIC_FORMSPREE_ID:
- Set in .env.local for local testing before upload
- OR set as environment variable in Hostinger panel (if available) and rebuild
- Without this, form appears but submissions go nowhere

From STATE.md open todos:
- Confirm deployment URL (domain root vs. subdirectory — affects basePath)
- Confirm client email provider (Outlook/M365 vs Gmail)
- Create Formspree account and get NEXT_PUBLIC_FORMSPREE_ID

Formspree + Outlook risk (from research):
Since May 2025, Microsoft enforces strict SPF/DKIM/DMARC. Default Formspree sender (noreply@formspree.io) may land in Outlook spam.
Mitigation: check Formspree plan for "Send from your domain" option; add client's inbox to safe senders as fallback.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Configure Formspree and do a final local form test</name>
  <what-built>
    All code is complete. The ContactSection form uses NEXT_PUBLIC_FORMSPREE_ID — set to 'placeholder' as fallback. Before deploying to production, set the real Formspree form ID and do a local submission test.
  </what-built>
  <how-to-verify>
    1. If Formspree account not yet created:
       - Go to formspree.io → create account → New Form
       - Set form name "Kolabore Contato"
       - Set notification email to client's actual email address
       - Copy the form ID (looks like: xpwzabcd)

    2. Set the env var locally:
       ```bash
       # Create or update .env.local in project root
       echo "NEXT_PUBLIC_FORMSPREE_ID=xpwzabcd" >> /c/GitOrius/Kolabore-site/.env.local
       ```
       Replace `xpwzabcd` with the actual form ID.

    3. Rebuild with the env var set:
       ```bash
       cd /c/GitOrius/Kolabore-site
       npm run build
       npx serve out -p 3001
       ```

    4. Submit the contact form at http://localhost:3001/contato with your own name and the CLIENT'S REAL EMAIL as the notification recipient (confirm Formspree settings). Fill in all fields and submit.

    5. Verify:
       - Form shows inline success message (not Formspree redirect page)
       - Formspree dashboard shows 1 submission received
       - Client's inbox receives the notification email
       - If using Outlook/M365: check spam/junk folder too

    6. If email goes to spam on Outlook:
       - Option A: In Formspree settings, enable "Send notifications from your domain" (requires Gold/Business plan) and follow DNS CNAME setup
       - Option B: Have client add formspree.io to Outlook safe senders as a temporary measure
       - Option C: Note this as a known limitation if plan level doesn't support custom domain

    7. Stop serve when done.
  </how-to-verify>
  <resume-signal>
    Type "formspree-ok" once:
    - NEXT_PUBLIC_FORMSPREE_ID is set in .env.local
    - Form submission received in Formspree dashboard
    - Notification email received in client's inbox (or spam situation documented)
    
    Include: "inbox-confirmed" or "spam-risk:[mitigation applied]"
  </resume-signal>
  <files>.env.local</files>
  <action>Human creates Formspree account, creates form, sets notification email to client's real address, copies form ID into .env.local as NEXT_PUBLIC_FORMSPREE_ID=&lt;id&gt;. Human rebuilds, serves locally, submits the form, and confirms receipt in client inbox.</action>
  <verify>
    <automated>grep -c NEXT_PUBLIC_FORMSPREE_ID .env.local</automated>
  </verify>
  <done>NEXT_PUBLIC_FORMSPREE_ID set in .env.local. Form submission confirmed in Formspree dashboard. Notification email received in client inbox.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Deploy to Hostinger</name>
  <what-built>
    Build is passing, form is configured. The out/ directory contains the complete static site ready for upload.
  </what-built>
  <how-to-verify>
    PRE-UPLOAD CHECKLIST:
    ```bash
    cd /c/GitOrius/Kolabore-site
    npm run build          # final clean build with real FORMSPREE_ID
    ls out/.htaccess       # must exist — Apache SPA fallback
    ls out/sitemap.xml     # must exist
    ls out/robots.txt      # must exist
    ls out/contato/        # must exist — the new /contato page
    ```
    All 4 `ls` commands must succeed before uploading.

    UPLOAD STEPS:
    Option A — FTP (recommended):
    1. Open FileZilla or another FTP client
    2. Connect using Hostinger FTP credentials from hPanel → Hosting → Manage → FTP Accounts
    3. Navigate to public_html/ on the remote server
    4. Upload ALL contents of the local `out/` directory into `public_html/`
       - IMPORTANT: Upload the CONTENTS of out/, not the out/ folder itself
       - After upload, public_html/ should contain index.html, .htaccess, sitemap.xml, robots.txt, etc.
    5. Ensure .htaccess is uploaded (FTP clients sometimes hide dot-files — enable "Show hidden files")

    Option B — File Manager (fallback):
    1. hPanel → Hosting → Manage → File Manager
    2. Navigate to public_html/
    3. Upload a ZIP of out/ contents, then extract in-place

    POST-UPLOAD:
    Verify .htaccess is present at public_html/.htaccess (not public_html/out/.htaccess).
  </how-to-verify>
  <resume-signal>
    Type "uploaded" once all out/ contents are in public_html/ and you can access https://kolabore.com.br in a browser.
  </resume-signal>
  <files>out/</files>
  <action>Human runs final npm run build (with real FORMSPREE_ID set). Human verifies out/.htaccess, out/sitemap.xml, out/robots.txt, out/contato/ all exist. Human uploads all out/ contents to public_html/ via FTP or Hostinger File Manager. Confirms .htaccess is at public_html/.htaccess (not in a subdirectory).</action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && ls out/.htaccess out/sitemap.xml out/robots.txt out/contato/</automated>
  </verify>
  <done>All out/ contents uploaded to Hostinger public_html/. Site accessible at https://kolabore.com.br.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Pre-launch validation checklist</name>
  <what-built>
    Site is live on Hostinger. This task verifies every item in the Phase 5 pre-launch checklist before the phase is marked complete.
  </what-built>
  <how-to-verify>
    Run through each item. Check off as you go. All items must pass.

    --- ROUTE HARD-REFRESH TEST ---
    Paste each URL directly into a browser address bar (do NOT navigate there from homepage — must be a direct URL load):
    - [ ] https://kolabore.com.br/ — loads correctly, no 404
    - [ ] https://kolabore.com.br/kolabore/ — loads correctly, no 404
    - [ ] https://kolabore.com.br/expertise/ — loads correctly, no 404
    - [ ] https://kolabore.com.br/engajamento/ — loads correctly, no 404
    - [ ] https://kolabore.com.br/executivos/ — loads correctly, no 404
    - [ ] https://kolabore.com.br/contato/ — loads correctly, no 404

    If any route returns 404: .htaccess SPA fallback is not active. Verify .htaccess is in public_html/ (not a subdirectory). Check Hostinger File Manager to confirm it's present. If Apache mod_rewrite is disabled, contact Hostinger support.

    --- .HTACCESS ---
    - [ ] https://kolabore.com.br/.htaccess is present (direct URL access may return 403 — that's fine, it means it exists)
    - [ ] All routes passed the hard-refresh test above (confirms rewrite rules are active)

    --- FORM DELIVERABILITY (CONT-08) ---
    - [ ] Submit the contact form at https://kolabore.com.br/contato/ with real data
    - [ ] Form shows inline success message (not a Formspree redirect page)
    - [ ] Client's inbox receives the submission notification
    - [ ] Check spam/junk if not in inbox within 5 minutes
    - [ ] If Outlook/M365: check Quarantine at https://security.microsoft.com

    --- OG IMAGE ---
    - [ ] Go to https://www.linkedin.com/post-inspector/
    - [ ] Paste https://kolabore.com.br/ and click "Inspect"
    - [ ] OG image renders (1200x630 Kolabore brand image appears)
    - [ ] Title and description appear correctly

    --- SITEMAP AND ROBOTS ---
    - [ ] https://kolabore.com.br/sitemap.xml loads and lists 6 URLs
    - [ ] https://kolabore.com.br/robots.txt loads and contains "Sitemap: https://kolabore.com.br/sitemap.xml"

    --- PAGE TITLES ---
    Open each page in browser. Check the browser tab for the title:
    - [ ] / — "Kolabore | Consultoria Executiva"
    - [ ] /kolabore/ — "A Kolabore | Kolabore"
    - [ ] /expertise/ — "Expertise | Kolabore"
    - [ ] /engajamento/ — "Engajamento | Kolabore"
    - [ ] /executivos/ — "Executivos | Kolabore"
    - [ ] /contato/ — "Contato | Kolabore"
    All must be unique.

    --- CONSOLE ERRORS ---
    Open each page. Open DevTools → Console. Look for red errors (warnings are OK, errors are not).
    - [ ] / — no console.error
    - [ ] /kolabore/ — no console.error
    - [ ] /expertise/ — no console.error
    - [ ] /engajamento/ — no console.error
    - [ ] /executivos/ — no console.error
    - [ ] /contato/ — no console.error

    --- MOBILE LAYOUT ---
    - [ ] Open DevTools → Toggle device emulation → iPhone SE (375px width)
    - [ ] Scroll through homepage — layout intact, no horizontal overflow, text readable
    - [ ] Scroll through /contato — form is full-width, all fields visible, submit button reachable
  </how-to-verify>
  <resume-signal>
    Type "launch-approved" once all checklist items pass.
    List any items that FAILED and how they were resolved.
    If form deliverability to Outlook is still unresolved, note it explicitly: "CONT-08-partial: Formspree delivers to Gmail; Outlook deliverability pending [reason]"
  </resume-signal>
  <files>out/</files>
  <action>Human executes all 9 pre-launch checklist categories on the live Hostinger domain: hard-refresh all 6 routes, verify .htaccess active, submit form and confirm inbox delivery, test OG image in LinkedIn Post Inspector, verify sitemap.xml and robots.txt accessible, confirm all 6 page titles unique, check console errors on all pages, test mobile layout at 375px.</action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://kolabore.com.br/sitemap.xml</automated>
  </verify>
  <done>All pre-launch checklist items passed. CONT-07 and CONT-08 confirmed. Phase 5 complete.</done>
</task>

</tasks>

<verification>
Phase 5 is complete when all three checkpoints are approved:
1. formspree-ok (Task 1) — NEXT_PUBLIC_FORMSPREE_ID set, form delivering to inbox
2. uploaded (Task 2) — out/ contents in public_html/
3. launch-approved (Task 3) — all 9 pre-launch checklist categories passed

CONT-07: All 6 routes survive hard refresh — confirmed in Task 3 route test
CONT-08: Form email in client inbox — confirmed in Task 1 (local) and Task 3 (live)
</verification>

<success_criteria>
- All 6 routes load with hard browser refresh on the live Hostinger domain (CONT-07)
- Contact form submission delivered to client's real inbox (CONT-08)
- .htaccess SPA fallback active at domain root
- sitemap.xml and robots.txt accessible at domain root
- OG image renders in LinkedIn Post Inspector
- All 6 page titles are unique
- No console.error on any page
- Mobile layout intact at 375px
- Phase 5 complete — v1 Kolabore site shipped
</success_criteria>

<output>
After completion, create `.planning/phases/05-contact-seo-deploy/05-05-SUMMARY.md`
</output>
