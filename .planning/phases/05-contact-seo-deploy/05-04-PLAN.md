---
phase: 05-contact-seo-deploy
plan: "04"
type: execute
wave: 2
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - src/app/page.tsx
  - src/app/executivos/page.tsx
  - src/components/sections/ExecutivosHeroSection.tsx
  - src/components/ui/ExecutiveCard.tsx
autonomous: false
requirements: []
must_haves:
  truths:
    - "Lighthouse mobile score on homepage is ≥ 90 after fixes"
    - "All portrait images have explicit width and height props — no CLS from unsized images"
    - "No render-blocking resources on the critical path that can be eliminated without removing features"
  artifacts:
    - path: "src/components/ui/ExecutiveCard.tsx"
      provides: "ExportedImage components with explicit width/height to prevent CLS"
  key_links:
    - from: "src/components/ui/ExecutiveCard.tsx"
      to: "ExportedImage (next-image-export-optimizer)"
      via: "width and height props set"
      pattern: "width=\\{|height=\\{"
---

<objective>
Run a Lighthouse mobile audit on the homepage and address any score below 90. The most likely culprits from research are portrait image CLS (missing width/height on ExportedImage), and minor JS or font loading issues. Then do a manual post-fix verification that score reaches ≥ 90.

Purpose: Phase 5 success criterion 3 requires Lighthouse mobile ≥ 90. CONT-06 (clean URLs) is confirmed automatically via build — trailingSlash is already set. The primary known risk is CLS from portrait images without explicit dimensions.
Output: Lighthouse mobile ≥ 90 on homepage. Any CLS-causing images have explicit width/height.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md

<interfaces>
<!-- Key patterns for image optimization in this codebase -->

next.config.ts (do not modify):
```typescript
images: {
  loader: "custom",   // ALL images must use ExportedImage, not next/image
  imageSizes: [16, 32, 48, 64, 96, 128, 256, 384],
  deviceSizes: [640, 750, 828, 1080, 1200, 1920, 2048, 3840],
},
```

Correct image import pattern (already used in ExecutiveCard — verify width/height):
```typescript
import ExportedImage from 'next-image-export-optimizer'
// ...
<ExportedImage
  src={exec.photo}
  alt={exec.name}
  width={400}    // MUST be set — prevents CLS
  height={400}   // MUST be set — prevents CLS
  className="..."
/>
```

ExportedImage with width/height generates blur placeholder (configured in next.config.ts:
  nextImageExportOptimizer_generateAndUseBlurImages: "true"
This means images below the fold get a blur placeholder and lazy loading automatically.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit ExportedImage components for missing width/height — fix any found</name>
  <files>
    src/components/ui/ExecutiveCard.tsx
    src/components/sections/TeamPreviewSection.tsx
  </files>
  <read_first>
    - src/components/ui/ExecutiveCard.tsx (read the full file — check every ExportedImage for width/height props)
    - src/components/sections/TeamPreviewSection.tsx (also uses team photos — check for ExportedImage usage)
    - src/app/executivos/page.tsx (check if any ExportedImage used directly in page)
  </read_first>
  <action>
Read each file listed above. For every `ExportedImage` or `<ExportedImage` found:

1. Check that both `width` and `height` props are present and numeric (not strings, not missing)
2. If either is missing, add them:
   - Portrait images (team photos): `width={400} height={400}` (square — matches the processed WebP dimensions)
   - Any other image: use actual pixel dimensions from the source file

Additionally, check if any `loading` prop is set incorrectly:
- The FIRST visible portrait (eager={true} or i===0 in the executives grid) should have `priority={true}` on ExportedImage to avoid LCP penalty
- All others should NOT have priority (lazy-loaded by default)

Also check for any `<img>` tags (native HTML, not ExportedImage) — if any exist on these pages, flag them in a comment and verify they have width/height attributes to prevent CLS.

If no changes are needed (all width/height already set), make no changes and document "verified — no changes needed" in the task output.

IMPORTANT: Do not change the visual layout, className, or src attributes. Only add missing width, height, or priority props to ExportedImage components.
  </action>
  <verify>
    <automated>cd /c/GitOrius/Kolabore-site && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Every ExportedImage in ExecutiveCard.tsx has both `width={` and `height={` props
    - Every ExportedImage in TeamPreviewSection.tsx (if any) has both `width={` and `height={` props
    - `npm run build` exits 0 after any changes
  </acceptance_criteria>
  <done>All portrait images have explicit width and height. Build passes. CLS risk from unsized images eliminated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run Lighthouse mobile audit and verify score ≥ 90</name>
  <what-built>
    Task 1 ensured all portrait images have explicit dimensions (CLS prevention). The build is clean. Now run a Lighthouse mobile audit on the homepage to confirm the score.
  </what-built>
  <how-to-verify>
    1. Run the production build and serve it locally:
       ```bash
       cd /c/GitOrius/Kolabore-site
       npm run build
       npx serve out -p 3001
       ```

    2. Open Chrome and navigate to http://localhost:3001

    3. Open DevTools → Lighthouse tab:
       - Mode: Navigation
       - Device: Mobile
       - Categories: Performance only (uncheck others to speed it up)
       - Click "Analyze page load"

    4. Wait for score. Expected: ≥ 90.

    5. If score is BELOW 90, check the "Opportunities" and "Diagnostics" sections:
       - **CLS (Cumulative Layout Shift)**: Images without dimensions — fix in ExecutiveCard or TeamPreviewSection
       - **LCP (Largest Contentful Paint)**: Hero image or heading — check if hero section has a preload hint or if fonts are loading correctly
       - **TBT / FID (Total Blocking Time)**: Large JS bundles — check if motion/react is being imported correctly (LazyMotion + domAnimation only, NOT the full bundle)
       - **FCP (First Contentful Paint)**: Font display swap or render-blocking CSS

       For motion/react bundle issues, search for incorrect imports:
       ```bash
       grep -r "from 'framer-motion'\|from 'motion/react'" src/ --include="*.tsx"
       ```
       All motion imports should be `from 'motion/react-m'` (NOT `from 'motion/react'`).

    6. Make fixes if needed, rebuild, and re-run Lighthouse until score ≥ 90.

    7. Stop the serve process (Ctrl+C) when done.
  </how-to-verify>
  <resume-signal>
    Type "approved SCORE:[number]" (e.g., "approved SCORE:94") once Lighthouse mobile score is ≥ 90.
    If fixes were made, briefly describe what was changed (e.g., "fixed motion import in HeroSection.tsx").
  </resume-signal>
  <files>src/components/ui/ExecutiveCard.tsx</files>
  <action>Human runs Lighthouse mobile audit on the local production build (npx serve out -p 3001). If score below 90, human diagnoses Opportunities section and applies fixes (motion import corrections, image dimension additions, font loading issues). Rebuilds and re-runs until score reaches >= 90.</action>
  <verify>
    <automated>npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>Lighthouse mobile score >= 90 confirmed on homepage. Build passes cleanly.</done>
</task>

</tasks>

<verification>
After checkpoint approval:
- Lighthouse mobile score reported ≥ 90
- npm run build exits 0
- All portrait ExportedImage components have width and height props
</verification>

<success_criteria>
- Lighthouse mobile performance score ≥ 90 on homepage
- No CLS from unsized images (all ExportedImage have width/height)
- Build passes cleanly
- Motion imports use 'motion/react-m' (LazyMotion pattern) — not full bundle
</success_criteria>

<output>
After completion, create `.planning/phases/05-contact-seo-deploy/05-04-SUMMARY.md`
</output>
