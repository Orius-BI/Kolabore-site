---
phase: 05-contact-seo-deploy
plan: "03"
type: execute
wave: 2
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - src/app/sitemap.ts
  - src/app/robots.ts
autonomous: true
requirements:
  - CONT-04
must_haves:
  truths:
    - "out/sitemap.xml is present after npm run build and lists all 6 URLs with trailing slashes"
    - "out/robots.txt is present after npm run build and points to sitemap.xml"
    - "Both files are generated by Next.js native file conventions — no third-party package installed"
  artifacts:
    - path: "src/app/sitemap.ts"
      provides: "Generates sitemap.xml in out/ at build time"
      contains: "export const dynamic = 'force-static'"
    - path: "src/app/robots.ts"
      provides: "Generates robots.txt in out/ at build time"
      contains: "export const dynamic = 'force-static'"
  key_links:
    - from: "src/app/sitemap.ts"
      to: "out/sitemap.xml"
      via: "Next.js App Router file convention with force-static"
      pattern: "force-static"
    - from: "src/app/robots.ts"
      to: "out/robots.txt"
      via: "Next.js App Router file convention with force-static"
      pattern: "force-static"
---

<objective>
Create `src/app/sitemap.ts` and `src/app/robots.ts` using the Next.js native file convention. Both files MUST include `export const dynamic = 'force-static'` — without it, `output: 'export'` silently skips both files (GitHub issue #68667, unfixed in Next.js 16.2.3).

Purpose: CONT-04 requires sitemap.xml and robots.txt in the build output. Zero new packages needed — native Next.js APIs handle this completely.
Output: `src/app/sitemap.ts`, `src/app/robots.ts`, verified in `out/` after build.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md

<interfaces>
<!-- Exact code patterns required. Do not deviate. -->

From research (Next.js 16.2.3 + output: 'export' bug fix):

CRITICAL CONSTRAINT: `output: 'export'` requires `export const dynamic = 'force-static'` in BOTH sitemap.ts and robots.ts. Without it, the build succeeds but the files are absent from out/. This is a known open bug (GitHub issue #68667).

next.config.ts (context — do not modify):
```typescript
const nextConfig: NextConfig = {
  output: "export",
  trailingSlash: true,   // ALL sitemap URLs must end with /
  // ...
}
```

Base URL confirmed: https://kolabore.com.br (from metadataBase in root layout)

The 6 routes with trailingSlash: true:
- https://kolabore.com.br/
- https://kolabore.com.br/kolabore/
- https://kolabore.com.br/expertise/
- https://kolabore.com.br/engajamento/
- https://kolabore.com.br/executivos/
- https://kolabore.com.br/contato/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/app/sitemap.ts</name>
  <files>src/app/sitemap.ts</files>
  <read_first>
    - next.config.ts (confirm trailingSlash: true and output: 'export' — must not modify)
    - src/app/layout.tsx (confirm metadataBase URL = https://kolabore.com.br)
  </read_first>
  <action>
Create `src/app/sitemap.ts` with this exact content — do not alter the `force-static` export or the URL structure:

```typescript
// src/app/sitemap.ts
// IMPORTANT: `export const dynamic = 'force-static'` is REQUIRED for output: 'export'.
// Without it, Next.js silently skips this file and out/sitemap.xml is never generated.
// See: https://github.com/vercel/next.js/issues/68667
import type { MetadataRoute } from 'next'

export const dynamic = 'force-static'

export default function sitemap(): MetadataRoute.Sitemap {
  const base = 'https://kolabore.com.br'
  const lastMod = new Date('2026-04-13')
  return [
    {
      url: `${base}/`,
      lastModified: lastMod,
      changeFrequency: 'monthly',
      priority: 1.0,
    },
    {
      url: `${base}/kolabore/`,
      lastModified: lastMod,
      changeFrequency: 'monthly',
      priority: 0.8,
    },
    {
      url: `${base}/expertise/`,
      lastModified: lastMod,
      changeFrequency: 'monthly',
      priority: 0.8,
    },
    {
      url: `${base}/engajamento/`,
      lastModified: lastMod,
      changeFrequency: 'monthly',
      priority: 0.8,
    },
    {
      url: `${base}/executivos/`,
      lastModified: lastMod,
      changeFrequency: 'monthly',
      priority: 0.7,
    },
    {
      url: `${base}/contato/`,
      lastModified: lastMod,
      changeFrequency: 'yearly',
      priority: 0.6,
    },
  ]
}
```

URLs use trailing slash because `trailingSlash: true` is set in next.config.ts. All 6 routes must be present.
  </action>
  <verify>
    <automated>cd /c/GitOrius/Kolabore-site && npm run build 2>&1 | tail -5 && ls out/sitemap.xml</automated>
  </verify>
  <acceptance_criteria>
    - src/app/sitemap.ts exists
    - src/app/sitemap.ts contains `export const dynamic = 'force-static'`
    - src/app/sitemap.ts contains `kolabore.com.br`
    - src/app/sitemap.ts contains all 6 URLs: `/`, `/kolabore/`, `/expertise/`, `/engajamento/`, `/executivos/`, `/contato/`
    - `npm run build` exits 0
    - `out/sitemap.xml` exists after build
    - `out/sitemap.xml` contains `kolabore.com.br/contato/` (confirms all 6 are present)
  </acceptance_criteria>
  <done>sitemap.ts created. out/sitemap.xml present after build with all 6 URLs.</done>
</task>

<task type="auto">
  <name>Task 2: Create src/app/robots.ts</name>
  <files>src/app/robots.ts</files>
  <read_first>
    - src/app/sitemap.ts (just created in Task 1 — confirm the sitemap URL to reference)
    - next.config.ts (confirm output: 'export' is active)
  </read_first>
  <action>
Create `src/app/robots.ts` with this exact content:

```typescript
// src/app/robots.ts
// IMPORTANT: `export const dynamic = 'force-static'` is REQUIRED for output: 'export'.
// Without it, Next.js silently skips this file and out/robots.txt is never generated.
// See: https://github.com/vercel/next.js/issues/68667
import type { MetadataRoute } from 'next'

export const dynamic = 'force-static'

export default function robots(): MetadataRoute.Robots {
  return {
    rules: {
      userAgent: '*',
      allow: '/',
    },
    sitemap: 'https://kolabore.com.br/sitemap.xml',
  }
}
```

Do not add `disallow` rules — the entire site is public and indexable.
  </action>
  <verify>
    <automated>cd /c/GitOrius/Kolabore-site && npm run build 2>&1 | tail -5 && ls out/sitemap.xml out/robots.txt</automated>
  </verify>
  <acceptance_criteria>
    - src/app/robots.ts exists
    - src/app/robots.ts contains `export const dynamic = 'force-static'`
    - src/app/robots.ts contains `sitemap: 'https://kolabore.com.br/sitemap.xml'`
    - `npm run build` exits 0
    - `out/robots.txt` exists after build
    - `out/sitemap.xml` still exists after build (not broken by adding robots.ts)
    - `out/robots.txt` contains `Sitemap: https://kolabore.com.br/sitemap.xml`
  </acceptance_criteria>
  <done>robots.ts created. out/robots.txt present after build and points to sitemap URL.</done>
</task>

</tasks>

<verification>
After both tasks:
```bash
cd /c/GitOrius/Kolabore-site
npm run build 2>&1 | tail -5
ls out/sitemap.xml out/robots.txt
grep "kolabore.com.br/contato" out/sitemap.xml
grep "Sitemap" out/robots.txt
```
All 4 commands must succeed. sitemap must contain /contato/ URL. robots.txt must list the Sitemap directive.
</verification>

<success_criteria>
- src/app/sitemap.ts exists with force-static export and all 6 URLs
- src/app/robots.ts exists with force-static export
- out/sitemap.xml present after npm run build
- out/robots.txt present after npm run build
- robots.txt Sitemap directive points to https://kolabore.com.br/sitemap.xml
- CONT-04 requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-contact-seo-deploy/05-03-SUMMARY.md`
</output>
