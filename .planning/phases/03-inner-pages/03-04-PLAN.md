---
phase: 03-inner-pages
plan: 04
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-03
files_modified:
  - src/app/engajamento/page.tsx
  - src/components/sections/engajamento/PageHeroSection.tsx
  - src/components/sections/engajamento/PhilosophySection.tsx
  - src/components/sections/engajamento/FormatsDeepSection.tsx
  - src/components/sections/engajamento/CustomArrangementsSection.tsx
  - src/components/sections/engajamento/WorkingRelationshipSection.tsx
  - src/components/sections/engajamento/EngajamentoCtaClose.tsx
autonomous: true
requirements:
  - PAGE-03

must_haves:
  truths:
    - "Visitor reading /engajamento understands how Kolabore actually works — when to use each format, how engagements run, what working with them feels like"
    - "Each of the 8 formats has a 'when to use' framing that names a client situation, not a product feature"
    - "Custom arrangements section explicitly names the flexibility differentiator as distinct from large-firm rigidity"
    - "Working relationship section sets expectations without corporate-speak"
    - "A secondary CTA to /contato appears after PhilosophySection, after FormatsDeepSection, and at page bottom"
    - "Build passes with no TypeScript or lint errors"
    - "npm run build produces out/ with no errors across all 5 public pages"
  artifacts:
    - path: "src/app/engajamento/page.tsx"
      provides: "Assembled /engajamento page"
    - path: "src/components/sections/engajamento/PageHeroSection.tsx"
      provides: "Page hero for /engajamento"
    - path: "src/components/sections/engajamento/PhilosophySection.tsx"
      provides: "Working philosophy — how Kolabore approaches engagements"
    - path: "src/components/sections/engajamento/FormatsDeepSection.tsx"
      provides: "All 8 formats with when-to-use + how-it-works + duration"
    - path: "src/components/sections/engajamento/CustomArrangementsSection.tsx"
      provides: "Custom arrangements differentiator section with CTA"
    - path: "src/components/sections/engajamento/WorkingRelationshipSection.tsx"
      provides: "3-item list of working relationship principles"
    - path: "src/components/sections/engajamento/EngajamentoCtaClose.tsx"
      provides: "Closing CTA section at page bottom"
  key_links:
    - from: "src/components/sections/engajamento/FormatsDeepSection.tsx"
      to: "src/data/formats.ts"
      via: "import { formats } from '@/data/formats'"
    - from: "src/components/sections/engajamento/FormatsDeepSection.tsx"
      to: "src/content/inner-pages.ts"
      via: "import { engajamentoPage } from '@/content/inner-pages'"
      pattern: "engajamentoPage.formatDetails\\[format.id\\]"
---

<objective>
Build the /engajamento page — the "how we work" page. Assembles six section components covering philosophy, all 8 engagement formats with depth, custom arrangement capability, and working relationship principles. Replaces the current placeholder page.tsx.

Purpose: The buyer who has decided to consider Kolabore needs to know if the engagement model fits. This page answers how, not just what.

Output: 6 component files in src/components/sections/engajamento/ and a wired page.tsx.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/content/inner-pages.ts
@src/data/formats.ts
@src/components/sections/kolabore/ValuePropositionSection.tsx
@src/components/sections/expertise/AreaDeepSection.tsx
@src/components/layout/Section.tsx
@src/components/layout/Container.tsx
@src/components/ui/Button.tsx
@src/components/ui/SectionHeading.tsx

<interfaces>
<!-- Copy from src/content/inner-pages.ts engajamentoPage object -->
<!-- Format data (id, label, description) from src/data/formats.ts -->
<!-- formatDetails keyed by format.id — each has: whenToUse, howItWorks, typicalDuration -->

From src/data/formats.ts:
```typescript
export interface EngagementFormat {
  id: string;       // e.g. "consultoria", "assessment", "quick-win"
  label: string;    // e.g. "Consultoria Completa"
  description: string;
}
export const formats: EngagementFormat[];  // 8 entries
```

From src/content/inner-pages.ts (engajamentoPage.formatDetails pattern):
```typescript
engajamentoPage.formatDetails["consultoria"] = {
  whenToUse: string,      // client situation framing
  howItWorks: string,     // 2–3 sentences on execution
  typicalDuration: string // e.g. "4 a 12 semanas"
}
```

From src/content/inner-pages.ts (engajamentoPage.workingRelationship pattern):
```typescript
engajamentoPage.workingRelationship.items = [
  { label: string, detail: string },  // 3 items
]
```

Established patterns from Plan 3.2 / 3.3 components:
- 'use client' + import * as m from 'motion/react-m'
- whileInView + viewport={{ once: true }}
- bg-ink / bg-mist alternating, text-gold accent, text-slate body, border-slate/20 dividers
- No hex values
- SectionHeading light={true} for bg-ink sections, light={false} for bg-mist
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build six /engajamento section components</name>
  <files>
    src/components/sections/engajamento/PageHeroSection.tsx
    src/components/sections/engajamento/PhilosophySection.tsx
    src/components/sections/engajamento/FormatsDeepSection.tsx
    src/components/sections/engajamento/CustomArrangementsSection.tsx
    src/components/sections/engajamento/WorkingRelationshipSection.tsx
    src/components/sections/engajamento/EngajamentoCtaClose.tsx
  </files>
  <action>
Create the directory `src/components/sections/engajamento/` and six component files. All are 'use client'. No hardcoded Portuguese strings.

**PageHeroSection.tsx** — Same structural pattern as kolabore and expertise PageHeroSection (bg-ink, pt-32 pb-20 lg:pt-40 lg:pb-28, h1 font-display, subheadline, primary Button). Import `engajamentoPage.pageHero`. Animate above-fold.

**PhilosophySection.tsx** — Light background (bg-mist). SectionHeading from `engajamentoPage.philosophy` (eyebrow + title + optional subtitle). Body paragraph (text-slate, max-w-3xl, leading-relaxed). At section bottom: secondary CTA `<Button variant="secondary" href="/contato">` with label from `engajamentoPage.philosophy.ctaLabel` if present, or "Fale com a Kolabore". Animate with whileInView.

**FormatsDeepSection.tsx** — Dark background (bg-ink). SectionHeading light={true} with eyebrow "Formatos de atuação" and title from `engajamentoPage` or a simple heading like "Como estruturamos cada projeto". 

For each format in the `formats` array (from src/data/formats.ts), render an expanded block using `engajamentoPage.formatDetails[format.id]`. Layout: two-column grid on lg (lg:grid-cols-2, gap-8), single column on mobile. Each format block:

```
Container block: border border-slate/20 rounded-[var(--radius-card)] p-6 bg-ink
[format.label] — <h3 className="font-semibold text-mist text-body">
[formatDetails.typicalDuration] — <span className="text-small text-gold uppercase tracking-wider">
[formatDetails.whenToUse] — <p className="mt-3 text-silver text-small leading-relaxed italic">  (italic to signal "when client situation = this")
[formatDetails.howItWorks] — <p className="mt-2 text-silver text-small leading-relaxed">
```

Stagger-animate format cards with whileInView. After the formats grid, add a secondary CTA row: `<Button variant="secondary" href="/contato">` with a short label like "Discuta o melhor formato para seu contexto".

**CustomArrangementsSection.tsx** — Light background (bg-mist). SectionHeading from `engajamentoPage.customArrangements` (eyebrow + title). Body paragraph. At bottom: `<Button variant="primary" href={engajamentoPage.customArrangements.ctaHref}>` with ctaLabel. This is a high-conversion section — use primary Button variant. Animate with whileInView.

**WorkingRelationshipSection.tsx** — Dark background (bg-ink). SectionHeading light={true} from `engajamentoPage.workingRelationship.eyebrow` and title. Render the 3 items as a vertical list of blocks (full-width, not a grid). Each item:

```
[item.label] — <h3 className="font-display text-h3 text-mist">
[item.detail] — <p className="mt-2 text-silver text-body leading-relaxed max-w-2xl">
```

Separate items with `<hr className="border-slate/20">`. Animate each item with staggered whileInView.

**EngajamentoCtaClose.tsx** — Closing section. Light background (bg-mist). Centered layout. Title from `engajamentoPage.ctaClose.title` as `<h2 className="font-display text-h2 text-ink text-center max-w-2xl mx-auto">`. Primary Button below: `<Button variant="primary" href={engajamentoPage.ctaClose.ctaHref}>` with ctaLabel. Animate fade-in.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>All six component files exist in src/components/sections/engajamento/, TypeScript clean, FormatsDeepSection correctly iterates formats[] and maps to engajamentoPage.formatDetails by format.id</done>
</task>

<task type="auto">
  <name>Task 2: Assemble /engajamento page.tsx and final build verification</name>
  <files>src/app/engajamento/page.tsx</files>
  <action>
Replace the placeholder src/app/engajamento/page.tsx:

```tsx
import { PageHeroSection } from '@/components/sections/engajamento/PageHeroSection';
import { PhilosophySection } from '@/components/sections/engajamento/PhilosophySection';
import { FormatsDeepSection } from '@/components/sections/engajamento/FormatsDeepSection';
import { CustomArrangementsSection } from '@/components/sections/engajamento/CustomArrangementsSection';
import { WorkingRelationshipSection } from '@/components/sections/engajamento/WorkingRelationshipSection';
import { EngajamentoCtaClose } from '@/components/sections/engajamento/EngajamentoCtaClose';

export default function EngajamentoPage() {
  return (
    <main>
      <PageHeroSection />
      <PhilosophySection />
      <FormatsDeepSection />
      <CustomArrangementsSection />
      <WorkingRelationshipSection />
      <EngajamentoCtaClose />
    </main>
  );
}
```

Server Component (no 'use client'). Run `npm run build`. This is the final build check for all three inner pages together (kolabore + expertise + engajamento).

If build fails: check TypeScript errors, fix imports, re-run build. Common issues to watch: missing exports in inner-pages.ts, format id mismatches between formats.ts and engajamentoPage.formatDetails, motion import paths.

Confirm `out/` directory contains: `out/kolabore/index.html`, `out/expertise/index.html`, `out/engajamento/index.html`.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>/engajamento page.tsx imports and renders all six sections, `npm run build` succeeds, out/ directory contains all three new inner page HTML files: out/kolabore/index.html, out/expertise/index.html, out/engajamento/index.html</done>
</task>

</tasks>

<verification>
- `npm run build` passes — out/ contains index.html for kolabore, expertise, and engajamento routes
- FormatsDeepSection renders all 8 formats — verify by checking imports formats array (length 8)
- Three CTA instances on the page: after PhilosophySection, after FormatsDeepSection grid, EngajamentoCtaClose at bottom
- CustomArrangementsSection uses primary Button variant (higher conversion intent)
- No hardcoded Portuguese strings in any component file
- All motion imports use `import * as m from 'motion/react-m'`
- `npx tsc --noEmit` clean
</verification>

<success_criteria>
All three inner pages (/kolabore, /expertise, /engajamento) build successfully and produce static HTML in out/. Each page has substantive copy beyond its homepage introduction, each major section closes with a CTA, and no "consultorês" generic language appears. Navigation through all pages via header links works.
</success_criteria>

<output>
After completion, create `.planning/phases/03-inner-pages/03-04-SUMMARY.md`
</output>
