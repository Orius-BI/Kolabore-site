---
phase: 03-inner-pages
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/app/kolabore/page.tsx
  - src/components/sections/kolabore/PageHeroSection.tsx
  - src/components/sections/kolabore/VisionSection.tsx
  - src/components/sections/kolabore/ValuePropositionSection.tsx
  - src/components/sections/kolabore/PositioningSection.tsx
  - src/components/sections/kolabore/SeniorityDeepSection.tsx
autonomous: true
requirements:
  - PAGE-01

must_haves:
  truths:
    - "Visitor reading /kolabore understands who Kolabore is, what they believe, and why to trust them — without having seen the homepage"
    - "Page adds substantive depth beyond the homepage hero and seniority sections — no duplicate sentences"
    - "Each major section ends with a secondary CTA linking to /contato or /executivos"
    - "Page renders without layout breaks at 375px mobile and 1440px desktop"
    - "Build passes with no TypeScript or lint errors"
  artifacts:
    - path: "src/app/kolabore/page.tsx"
      provides: "Assembled /kolabore page wiring all section components"
    - path: "src/components/sections/kolabore/PageHeroSection.tsx"
      provides: "Page hero for /kolabore"
    - path: "src/components/sections/kolabore/VisionSection.tsx"
      provides: "Vision statement section"
    - path: "src/components/sections/kolabore/ValuePropositionSection.tsx"
      provides: "Value proposition with 3–4 labeled items"
    - path: "src/components/sections/kolabore/PositioningSection.tsx"
      provides: "Positioning narrative with secondary CTA"
    - path: "src/components/sections/kolabore/SeniorityDeepSection.tsx"
      provides: "Seniority depth section with CTA to /executivos"
  key_links:
    - from: "src/app/kolabore/page.tsx"
      to: "src/components/sections/kolabore/*"
      via: "named imports, rendered in order"
    - from: "src/components/sections/kolabore/*"
      to: "src/content/inner-pages.ts"
      via: "import { kolaborePage } from '@/content/inner-pages'"
---

<objective>
Build the /kolabore page by assembling five section components that expand on the firm's identity, vision, value proposition, positioning, and seniority. Replaces the current placeholder page.tsx.

Purpose: This is the "who we are" page — the visitor who wants to understand Kolabore deeply before contacting them. It must answer all credibility questions without falling back to generic consulting language.

Output: 5 section components in src/components/sections/kolabore/ and a wired page.tsx.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/content/inner-pages.ts
@src/content/homepage.ts
@src/components/sections/HeroSection.tsx
@src/components/sections/AreasSection.tsx
@src/components/layout/Section.tsx
@src/components/layout/Container.tsx
@src/components/ui/Button.tsx
@src/components/ui/SectionHeading.tsx

<interfaces>
<!-- All copy comes from src/content/inner-pages.ts — do NOT hardcode strings in components. -->
<!-- Motion pattern: 'use client' + import * as m from 'motion/react-m' + whileInView + viewport once:true -->
<!-- Design tokens: bg-ink (near-black), bg-mist (warm off-white), text-gold (accent), text-slate (muted), border-slate/20 -->
<!-- No hex values — Tailwind tokens only -->

From src/components/ui/Button.tsx:
```typescript
<Button variant="primary" href="/contato">CTA label</Button>
<Button variant="secondary" href="/engajamento">Secondary CTA</Button>
<Button variant="ghost" href="/executivos">Ghost CTA</Button>
```

From src/components/ui/SectionHeading.tsx:
```typescript
<SectionHeading eyebrow="..." title="..." subtitle="..." align="left" light={false} />
<SectionHeading eyebrow="..." title="..." align="left" light={true} />  // for dark bg sections
```

From src/components/layout/Section.tsx:
```typescript
<Section id="vision" className="bg-mist">...</Section>
<Section id="positioning" className="bg-ink">...</Section>
```

Motion pattern (from AreasSection.tsx):
```typescript
'use client';
import * as m from 'motion/react-m';
// whileInView animation:
<m.div
  initial={{ opacity: 0, y: 20 }}
  whileInView={{ opacity: 1, y: 0 }}
  viewport={{ once: true }}
  transition={{ duration: 0.5, ease: 'easeOut' }}
>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build five /kolabore section components</name>
  <files>
    src/components/sections/kolabore/PageHeroSection.tsx
    src/components/sections/kolabore/VisionSection.tsx
    src/components/sections/kolabore/ValuePropositionSection.tsx
    src/components/sections/kolabore/PositioningSection.tsx
    src/components/sections/kolabore/SeniorityDeepSection.tsx
  </files>
  <action>
Create the directory `src/components/sections/kolabore/` and five component files. All components are 'use client'. All copy comes from `import { kolaborePage } from '@/content/inner-pages'`. No hardcoded Portuguese strings in components.

**PageHeroSection.tsx** — Page hero. Dark background (bg-ink). Full-width layout like HeroSection.tsx but without min-h-screen (use pt-32 pb-20 lg:pt-40 lg:pb-28). Animate headline + subheadline with `m.div` initial/animate (not whileInView — it's above the fold). Render `kolaborePage.pageHero.headline` as `<h1>` with font-display class. Render subheadline as `<p>`. Render a single `<Button variant="primary" href={kolaborePage.pageHero.ctaHref}>` with the ctaLabel. No secondary CTA on the hero — save secondary CTAs for section bottoms.

**VisionSection.tsx** — Light background (bg-mist). `<SectionHeading eyebrow light={false}>` with kolaborePage.vision.eyebrow and title. Body text rendered as `<p className="text-body text-slate max-w-2xl leading-relaxed">`. Animate with whileInView. No CTA needed in this section (the vision section builds context for positioning below).

**ValuePropositionSection.tsx** — Dark background (bg-ink). SectionHeading with `light={true}`. Render `kolaborePage.valueProposition.items` as a 2-col grid (lg:grid-cols-2, gap-6). Each item: a card-like block (border border-gold/20 rounded-[var(--radius-card)] p-6) with `<p className="font-semibold text-mist text-body">` for label and `<p className="mt-2 text-silver text-small leading-relaxed">` for detail. Stagger animate children with whileInView. At the bottom of the section (below the grid), add a subtle secondary CTA row: `<Button variant="secondary" href="/contato">` with kolaborePage.valueProposition ctaLabel if present, otherwise use "Fale com a Kolabore".

**PositioningSection.tsx** — Light background (bg-mist). SectionHeading with `light={false}`. Body paragraph (text-slate). At bottom of section: `<Button variant="secondary" href={kolaborePage.positioning.ctaHref}>` with ctaLabel. Animate with whileInView.

**SeniorityDeepSection.tsx** — Dark background (bg-ink). SectionHeading with `light={true}`. Body paragraph (text-silver). At bottom of section: `<Button variant="ghost" href={kolaborePage.seniority.ctaHref}>` with ctaLabel (links to /executivos). Animate with whileInView.

All components must follow: `import * as m from 'motion/react-m'` — never import from 'motion/react' directly. Use `viewport={{ once: true }}` on all whileInView animations.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>All five component files exist in src/components/sections/kolabore/, TypeScript compiles clean, no hardcoded copy strings in any component file</done>
</task>

<task type="auto">
  <name>Task 2: Assemble /kolabore page.tsx and verify build</name>
  <files>src/app/kolabore/page.tsx</files>
  <action>
Replace the placeholder src/app/kolabore/page.tsx with the assembled page. Import all five section components and render them in order:

```tsx
import { PageHeroSection } from '@/components/sections/kolabore/PageHeroSection';
import { VisionSection } from '@/components/sections/kolabore/VisionSection';
import { ValuePropositionSection } from '@/components/sections/kolabore/ValuePropositionSection';
import { PositioningSection } from '@/components/sections/kolabore/PositioningSection';
import { SeniorityDeepSection } from '@/components/sections/kolabore/SeniorityDeepSection';

export default function KolaborePage() {
  return (
    <main>
      <PageHeroSection />
      <VisionSection />
      <ValuePropositionSection />
      <PositioningSection />
      <SeniorityDeepSection />
    </main>
  );
}
```

This is a Server Component (no 'use client' on page.tsx — the individual section components carry their own 'use client' declarations).

After wiring, run `npm run build` to confirm the full static export compiles without errors. Fix any TypeScript or import errors before marking done.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>/kolabore page.tsx imports and renders all five sections, `npm run build` succeeds producing an updated out/ directory with no errors</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- `src/app/kolabore/page.tsx` contains no hardcoded Portuguese strings (grep check)
- All 5 section files exist in src/components/sections/kolabore/
- Each section component uses `import * as m from 'motion/react-m'`
- At least 3 of the 5 sections have a Button CTA (positioning → /engajamento, value-proposition → /contato, seniority → /executivos)
</verification>

<success_criteria>
/kolabore page renders all five sections with substantive copy from inner-pages.ts. Build passes. Each deep-content section closes with a secondary CTA. No layout breaks.
</success_criteria>

<output>
After completion, create `.planning/phases/03-inner-pages/03-02-SUMMARY.md`
</output>
