---
phase: 03-inner-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/inner-pages.ts
autonomous: true
requirements:
  - PAGE-01
  - PAGE-02
  - PAGE-03

must_haves:
  truths:
    - "All copy for /kolabore, /expertise, and /engajamento exists as typed TypeScript constants"
    - "No heading on any of the three pages could appear unchanged on a generic consulting site (substitution test)"
    - "Inner page copy adds depth beyond the homepage introduction — zero sentence duplication with homepage.ts"
    - "Commercial framing throughout: executive problems and outcomes, not service catalog language"
  artifacts:
    - path: "src/content/inner-pages.ts"
      provides: "All copy constants for the three inner pages"
      exports:
        - "kolaborePage"
        - "expertisePage"
        - "engajamentoPage"
  key_links:
    - from: "src/content/inner-pages.ts"
      to: "src/app/kolabore/page.tsx"
      via: "named import of kolaborePage"
    - from: "src/content/inner-pages.ts"
      to: "src/app/expertise/page.tsx"
      via: "named import of expertisePage"
    - from: "src/content/inner-pages.ts"
      to: "src/app/engajamento/page.tsx"
      via: "named import of engajamentoPage"
---

<objective>
Write all copy for the three inner pages as tested TypeScript constants in a single content file. This is the content foundation that Plans 3.2, 3.3, and 3.4 will consume — no component work is done here.

Purpose: Decouple copywriting from component implementation. Enforces the substitution test discipline before any markup exists. Follows the same pattern established in Plan 2.1 (src/content/homepage.ts).

Output: src/content/inner-pages.ts with three exported const objects, each fully typed and substitution-tested.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/content/homepage.ts
@docs/previa_do_prd.md

<interfaces>
<!-- Reuse the established homepage content pattern. Components import named consts. -->
<!-- Each exported object holds all string constants for its page. No logic, no JSX. -->

From src/content/homepage.ts (pattern to follow):
```typescript
export const hero = {
  headline: "...",
  subheadline: "...",
  ctaPrimary: "...",
} as const;

export const areas = {
  sectionTitle: "...",
  subtitle: "...",
} as const;
```

From src/data/areas.ts (8 entries — do NOT duplicate, reference by id in copy):
  supply-chain, operacoes, procurement, governanca, qualidade, projetos, inovacao, conselho

From src/data/formats.ts (8 entries — do NOT duplicate, reference by id in copy):
  consultoria, assessment, benchmark, quick-win, workshop, mentoring, comites, custom
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write inner-pages.ts with all three page copy objects</name>
  <files>src/content/inner-pages.ts</files>
  <action>
Create src/content/inner-pages.ts exporting three typed const objects. Follow the homepage.ts pattern exactly — string constants only, `as const`, no JSX, no logic.

File header comment must state: "All copy passes substitution test: replacing 'Kolabore' with a competitor name must break or change the sentence meaning."

**`export const kolaborePage`** — copy for /kolabore page:

Sections to include:

1. `pageHero`: eyebrow "Kolabore", headline (strong declarative about the firm's identity — not just "Quem somos"), subheadline (1–2 sentences on the firm's reason for existing), ctaLabel "Converse com a Kolabore", ctaHref "/contato"

2. `vision`: eyebrow "Visão", title (the firm's orienting conviction — what they believe about how great businesses are built or how executives should get support), body (2–3 sentences expanding the vision — why Kolabore exists as an alternative to the large generic consultancy model). Must be Kolabore-specific: reference the founder/senior executive model, the practical orientation, the "we have operated businesses, not just studied them" angle.

3. `valueProposition`: eyebrow "Proposta de valor", title (outcome-oriented — what the client gets, not a list of services), items array (3–4 short items each with `label` and `detail` — e.g., "Senioridade real": "Executivos que já ocuparam as posições que você ocupa hoje..."; "Arranjos sob medida": "..."; "Visão transversal": "..."; "Orientação a valor": "...")

4. `positioning`: eyebrow "Posicionamento", title (where Kolabore sits relative to the market — premium, specialized, executive-level — phrased as a statement, not a claim), body (2–3 sentences on what distinguishes Kolabore from large generic consultancies AND from boutiques that lack depth — the specific combination of seniority + breadth + practical experience), ctaLabel "Saiba como atuamos", ctaHref "/engajamento"

5. `seniority`: eyebrow "Senioridade", title (declares the firm's human capital as its primary asset — NOT the same as the homepage SenioritySection which uses metrics), body (2–3 sentences on WHY seniority matters for the client's specific decisions — framed as the client's problem, not Kolabore's biography), ctaLabel "Conheça nossa equipe", ctaHref "/executivos"

**`export const expertisePage`** — copy for /expertise page:

Sections to include:

1. `pageHero`: eyebrow "Expertise", headline (what problems Kolabore exists to solve — executive challenge framing, not a capability list), subheadline (1–2 sentences on depth and practicality), ctaLabel "Converse com a Kolabore", ctaHref "/contato"

2. `intro`: eyebrow "O que resolvemos", title (reframes expertise as a commitment to the client's specific problem, not a menu), body (2–3 sentences — make explicit that Kolabore does NOT do all things for all clients; they bring depth in specific, complementary fronts)

3. `areaDetails`: an object keyed by area id (matching src/data/areas.ts ids exactly: supply-chain, operacoes, procurement, governanca, qualidade, projetos, inovacao, conselho). Each entry has:
   - `challengeTitle`: a question or statement naming the executive challenge in that area (e.g. for supply-chain: "Quando a cadeia de suprimento vira um problema de negócio")
   - `depth`: 2–4 sentences expanding significantly beyond the one-liner in areas.ts — add concrete types of situations, what goes wrong, what Kolabore brings, what outcome looks like. Substitution test applies: must reference details specific to Kolabore's approach.
   - `examples`: array of 2–3 short strings naming specific sub-problems or situations (e.g. ["Redesenho de rede de distribuição", "Gestão de ruptura em picos de demanda", "Redução de custo total de cadeia"])

4. `ctaBanner`: a section-level CTA that appears between areas — title (short, executive tone), ctaLabel "Agende uma conversa", ctaHref "/contato"

**`export const engajamentoPage`** — copy for /engajamento page:

Sections to include:

1. `pageHero`: eyebrow "Engajamento", headline (how Kolabore works — framed around the client's flexibility needs and the absence of one-size-fits-all), subheadline (1–2 sentences), ctaLabel "Converse com a Kolabore", ctaHref "/contato"

2. `philosophy`: eyebrow "Como trabalhamos", title (a statement about Kolabore's working philosophy — proximity, result-orientation, no unnecessary overhead), body (2–3 sentences — cover: they start with the problem, not a predefined solution; they adapt the team composition to the challenge; they stay through implementation, not just diagnosis)

3. `formatDetails`: an object keyed by format id (matching src/data/formats.ts ids exactly: consultoria, assessment, benchmark, quick-win, workshop, mentoring, comites, custom). Each entry has:
   - `whenToUse`: 1–2 sentences describing the client situation that calls for this format (problem-first, not feature description)
   - `howItWorks`: 2–3 sentences on how Kolabore executes this format — specific, not generic consulting language
   - `typicalDuration`: short string (e.g. "4 a 12 semanas", "1 a 3 dias", "Recorrente, mensal ou bimestral")

4. `customArrangements`: eyebrow "Arranjos customizados", title (the ability to combine formats and people is a differentiator — name it explicitly), body (2–3 sentences on how Kolabore builds cross-functional teams from the group, brings ad hoc partners when needed, and sizes the engagement to the actual problem), ctaLabel "Fale com a Kolabore", ctaHref "/contato"

5. `workingRelationship`: eyebrow "Como é trabalhar com a Kolabore", title (sets expectations for the client relationship — executive-level, direct, oriented to outcomes), items array (3 items each with `label` and `detail` — e.g., "Acesso direto": "Você trabalha com os seniores, não com uma equipe júnior que faz relay."; "Foco em implementação": "Não entregamos relatório e saímos. Ficamos enquanto o problema precisar de solução."; "Sem overhead desnecessário": "Engajamentos dimensionados para o problema real, não para maximizar horas.")

6. `ctaClose`: title (closing statement — invitation to start a conversation, not a sales pitch), ctaLabel "Agende uma conversa", ctaHref "/contato"

**Substitution test (MANDATORY):** After writing, mentally replace "Kolabore" with "McKinsey", "Deloitte", or "uma consultoria". If ANY heading or body text still makes sense unchanged, rewrite it until it breaks. The content must reference the specific senior executive model, the practical orientation, and the specific areas of focus.

**Do NOT:** repeat any sentence from homepage.ts. Cross-check all `sectionTitle`/headline values in homepage.ts before finalizing.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>src/content/inner-pages.ts exists, TypeScript compiles without errors, file exports kolaborePage, expertisePage, and engajamentoPage with all sections described above populated with non-generic, substitution-tested Portuguese copy</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- File exports three named consts: kolaborePage, expertisePage, engajamentoPage
- Grep confirms zero sentence overlap with src/content/homepage.ts key headings
- All area ids in areaDetails match exactly: supply-chain, operacoes, procurement, governanca, qualidade, projetos, inovacao, conselho
- All format ids in formatDetails match exactly: consultoria, assessment, benchmark, quick-win, workshop, mentoring, comites, custom
</verification>

<success_criteria>
src/content/inner-pages.ts is committed, TypeScript clean, with all three page copy objects fully populated. Plans 3.2, 3.3, and 3.4 can import from this file without needing to write any copy themselves.
</success_criteria>

<output>
After completion, create `.planning/phases/03-inner-pages/03-01-SUMMARY.md`
</output>
