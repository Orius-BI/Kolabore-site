---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
files_modified:
  - src/components/layout/Header.tsx
  - src/components/layout/Footer.tsx
  - src/components/layout/Container.tsx
  - src/components/layout/Section.tsx
  - src/app/layout.tsx
autonomous: false
requirements:
  - FOUND-08

must_haves:
  truths:
    - "Header renders on every page with fixed positioning"
    - "Header is transparent at top of page and transitions to `bg-ink/95` with backdrop blur when user scrolls past 60px"
    - "Header contains navigation links: Kolabore / Expertise / Engajamento / Executivos / Contato, and a CTA button 'Converse com a Kolabore'"
    - "Footer renders on every page below main content with company name and nav links"
    - "Container provides `max-w-7xl mx-auto px-6 lg:px-12` padding wrapper"
    - "Section provides semantic `<section>` wrapper with `py-20 lg:py-28` and optional `id` anchor"
    - "All 4 layout components are wired into root layout.tsx"
    - "`npm run build` passes with all 4 components present"
  artifacts:
    - path: "src/components/layout/Header.tsx"
      provides: "Fixed header with scroll-triggered background"
      contains: "'use client'"
    - path: "src/components/layout/Footer.tsx"
      provides: "Site footer"
    - path: "src/components/layout/Container.tsx"
      provides: "max-width content wrapper"
    - path: "src/components/layout/Section.tsx"
      provides: "Semantic section wrapper with spacing"
    - path: "src/app/layout.tsx"
      provides: "Root layout wired with Header + Footer"
      contains: "<Header />"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/layout/Header.tsx"
      via: "import and render inside <body>"
      pattern: "import.*Header"
    - from: "src/components/layout/Header.tsx"
      to: "src/styles/globals.css tokens"
      via: "Tailwind classes: bg-ink, text-mist, text-gold, border-slate"
      pattern: "bg-ink|text-gold|border-slate"
---

<objective>
Build the four layout primitives — Header, Footer, Container, Section — and wire Header + Footer into the root layout.

Purpose: These are the structural components every page in the site depends on. The Header scroll behavior (transparent to solid) must work in production — it uses a `useState` + `window.addEventListener('scroll')` pattern with `{ passive: true }` for performance. Plan 1.5 (route scaffold) and all of Phase 2 depend on these being in place.

Output: Four components in `src/components/layout/`, Header and Footer wired into `src/app/layout.tsx`. A human-verify checkpoint confirms the scroll behavior works in a browser before the plan is marked complete.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</context>

<interfaces>
<!-- Key patterns from research for executor — no codebase exploration needed -->

From 01-RESEARCH.md Pattern 7 (Header):
```tsx
"use client";
import { useState, useEffect } from "react";
import Link from "next/link";

export function Header() {
  const [scrolled, setScrolled] = useState(false);

  useEffect(() => {
    const onScroll = () => setScrolled(window.scrollY > 60);
    window.addEventListener("scroll", onScroll, { passive: true });
    return () => window.removeEventListener("scroll", onScroll);
  }, []);

  return (
    <header
      className={`fixed top-0 inset-x-0 z-50 transition-colors duration-300 ${
        scrolled
          ? "bg-ink/95 backdrop-blur-sm border-b border-slate/30"
          : "bg-transparent"
      }`}
    >
      {/* nav content here */}
    </header>
  );
}
```

From 01-RESEARCH.md Pattern 8 (Container + Section):
```tsx
// Container: max-w-7xl mx-auto px-6 lg:px-12
// Section: py-20 lg:py-28 with optional id and className props
```

Color tokens available (from Plan 1.2 globals.css):
- bg-ink (#0d0d0d), bg-carbon (#1a1a1a)
- text-mist (#f5f5f4), text-silver (#8a8a8a)
- text-gold (#b8933f), hover:text-gold-hover (#d4ad6b)
- border-slate (#2c2c2c)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build Container, Section, Footer, and Header components</name>

  <read_first>
    - `.planning/phases/01-foundation/01-RESEARCH.md` — Pattern 7 (Header scroll behavior, complete code), Pattern 8 (Container and Section code), Anti-Patterns (why not CSS scroll-driven for header)
    - `src/styles/globals.css` (verify which color token names are available before writing className strings)
    - `src/app/layout.tsx` (current state — you will modify it in Task 2 to add Header/Footer)
  </read_first>

  <files>
    src/components/layout/Container.tsx
    src/components/layout/Section.tsx
    src/components/layout/Footer.tsx
    src/components/layout/Header.tsx
  </files>

  <action>
    Create `src/components/layout/` directory if it doesn't exist.

    **File 1: `src/components/layout/Container.tsx`**
    ```tsx
    // src/components/layout/Container.tsx
    interface ContainerProps {
      children: React.ReactNode;
      className?: string;
    }

    export function Container({ children, className = "" }: ContainerProps) {
      return (
        <div className={`max-w-7xl mx-auto px-6 lg:px-12 ${className}`}>
          {children}
        </div>
      );
    }
    ```

    **File 2: `src/components/layout/Section.tsx`**
    ```tsx
    // src/components/layout/Section.tsx
    interface SectionProps {
      id?: string;
      children: React.ReactNode;
      className?: string;
    }

    export function Section({ id, children, className = "" }: SectionProps) {
      return (
        <section id={id} className={`py-20 lg:py-28 ${className}`}>
          {children}
        </section>
      );
    }
    ```

    **File 3: `src/components/layout/Footer.tsx`**
    ```tsx
    // src/components/layout/Footer.tsx
    import Link from "next/link";
    import { Container } from "./Container";

    const navLinks = [
      { label: "Kolabore", href: "/kolabore" },
      { label: "Expertise", href: "/expertise" },
      { label: "Engajamento", href: "/engajamento" },
      { label: "Executivos", href: "/executivos" },
      { label: "Contato", href: "/contato" },
    ];

    export function Footer() {
      return (
        <footer className="bg-carbon border-t border-slate/30 py-12">
          <Container>
            <div className="flex flex-col md:flex-row items-start md:items-center justify-between gap-8">
              <div>
                <p className="text-mist font-display text-xl font-medium tracking-wide">
                  Kolabore
                </p>
                <p className="text-silver text-sm mt-1">
                  Consultoria Executiva
                </p>
              </div>
              <nav aria-label="Rodapé">
                <ul className="flex flex-wrap gap-6">
                  {navLinks.map((link) => (
                    <li key={link.href}>
                      <Link
                        href={link.href}
                        className="text-silver text-sm hover:text-mist transition-colors duration-200"
                      >
                        {link.label}
                      </Link>
                    </li>
                  ))}
                </ul>
              </nav>
            </div>
            <div className="mt-8 pt-8 border-t border-slate/20">
              <p className="text-silver text-xs">
                © {new Date().getFullYear()} Kolabore. Todos os direitos reservados.
              </p>
            </div>
          </Container>
        </footer>
      );
    }
    ```

    **File 4: `src/components/layout/Header.tsx`**
    ```tsx
    // src/components/layout/Header.tsx
    // 'use client' is required — uses useState and addEventListener for scroll detection
    "use client";
    import { useState, useEffect } from "react";
    import Link from "next/link";
    import { Container } from "./Container";

    const navLinks = [
      { label: "Kolabore", href: "/kolabore" },
      { label: "Expertise", href: "/expertise" },
      { label: "Engajamento", href: "/engajamento" },
      { label: "Executivos", href: "/executivos" },
      { label: "Contato", href: "/contato" },
    ];

    export function Header() {
      const [scrolled, setScrolled] = useState(false);

      useEffect(() => {
        const onScroll = () => setScrolled(window.scrollY > 60);
        window.addEventListener("scroll", onScroll, { passive: true });
        return () => window.removeEventListener("scroll", onScroll);
      }, []);

      return (
        <header
          className={`fixed top-0 inset-x-0 z-50 transition-colors duration-300 ${
            scrolled
              ? "bg-ink/95 backdrop-blur-sm border-b border-slate/30"
              : "bg-transparent"
          }`}
        >
          <Container>
            <div className="flex items-center justify-between h-16 lg:h-20">
              <Link
                href="/"
                className="text-mist font-display text-lg font-medium tracking-wide hover:text-gold transition-colors duration-200"
              >
                Kolabore
              </Link>
              <nav className="hidden md:flex items-center gap-8" aria-label="Principal">
                {navLinks.map((link) => (
                  <Link
                    key={link.href}
                    href={link.href}
                    className="text-silver text-sm hover:text-mist transition-colors duration-200"
                  >
                    {link.label}
                  </Link>
                ))}
              </nav>
              <Link
                href="/contato"
                className="hidden md:inline-flex items-center px-4 py-2 text-sm font-medium text-ink bg-gold hover:bg-gold-hover transition-colors duration-200 rounded-[var(--radius-button)]"
              >
                Converse com a Kolabore
              </Link>
            </div>
          </Container>
        </header>
      );
    }
    ```

    Run build after creating all 4 files (before wiring into layout):
    ```bash
    npm run build
    ```
    Expect: build passes. If TypeScript errors about missing modules, fix import paths.
  </action>

  <verify>
    <automated>cd /c/GitOrius/Kolabore-site && test -f src/components/layout/Header.tsx && test -f src/components/layout/Footer.tsx && test -f src/components/layout/Container.tsx && test -f src/components/layout/Section.tsx && grep -c "'use client'" src/components/layout/Header.tsx && grep -c "window.scrollY > 60" src/components/layout/Header.tsx && grep -c "Converse com a Kolabore" src/components/layout/Header.tsx</automated>
  </verify>

  <acceptance_criteria>
    - All 4 files exist: `src/components/layout/{Header,Footer,Container,Section}.tsx`
    - `Header.tsx` has `"use client"` directive at top
    - `Header.tsx` contains `window.scrollY > 60` scroll threshold
    - `Header.tsx` contains `{ passive: true }` in addEventListener call
    - `Header.tsx` contains all 5 nav links: `/kolabore`, `/expertise`, `/engajamento`, `/executivos`, `/contato`
    - `Header.tsx` contains the CTA link with text `"Converse com a Kolabore"`
    - `Header.tsx` contains `bg-ink/95` and `backdrop-blur-sm` for scrolled state
    - `Container.tsx` contains `max-w-7xl mx-auto px-6 lg:px-12`
    - `Section.tsx` contains `py-20 lg:py-28`
    - `npm run build` exits 0
  </acceptance_criteria>

  <done>All 4 layout components created; Header has scroll behavior with passive listener and CTA; build passes</done>
</task>

<task type="auto">
  <name>Task 2: Wire Header and Footer into root layout.tsx</name>

  <read_first>
    - `src/app/layout.tsx` (current state — you will add Header and Footer imports and usage)
    - `src/components/layout/Header.tsx` (verify export name is `Header`)
    - `src/components/layout/Footer.tsx` (verify export name is `Footer`)
    - `.planning/phases/01-foundation/01-RESEARCH.md` — Pattern 4 (layout.tsx with Header/Footer wired)
  </read_first>

  <files>
    src/app/layout.tsx
  </files>

  <action>
    Update `src/app/layout.tsx` to import and render Header and Footer.

    Add these two imports after the existing imports:
    ```tsx
    import { Header } from "@/components/layout/Header";
    import { Footer } from "@/components/layout/Footer";
    ```

    Update the JSX inside `<body>` to wrap children with Header and Footer:
    ```tsx
    <body className="font-sans bg-ink text-mist antialiased">
      <AnimationProvider>
        <Header />
        <main>{children}</main>
        <Footer />
      </AnimationProvider>
    </body>
    ```

    Do NOT change the `<html>` element, font variables, `lang`, `metadata`, or AnimationProvider import — only add the two layout component imports and update the body JSX.

    Run build to verify:
    ```bash
    npm run build
    ```
  </action>

  <verify>
    <automated>cd /c/GitOrius/Kolabore-site && grep -c "import.*Header.*from" src/app/layout.tsx && grep -c "import.*Footer.*from" src/app/layout.tsx && grep -c "<Header" src/app/layout.tsx && grep -c "<Footer" src/app/layout.tsx && npm run build 2>&1 | tail -3</automated>
  </verify>

  <acceptance_criteria>
    - `src/app/layout.tsx` imports `Header` from `"@/components/layout/Header"`
    - `src/app/layout.tsx` imports `Footer` from `"@/components/layout/Footer"`
    - `src/app/layout.tsx` renders `<Header />` before `<main>`
    - `src/app/layout.tsx` renders `<Footer />` after `<main>`
    - Both Header and Footer are inside `<AnimationProvider>`
    - `npm run build` exits 0
    - `out/index.html` exists and contains `"Kolabore"` text (grep verification)
  </acceptance_criteria>

  <done>Header and Footer wired into root layout; build passes; all pages will render nav and footer</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Four layout components (Header, Footer, Container, Section) wired into root layout. The critical behavior to verify is the Header scroll transition: it starts transparent and becomes solid `bg-ink` with backdrop blur after scrolling 60px.
  </what-built>
  <how-to-verify>
    1. Run the static build and serve it locally:
       ```bash
       cd C:/GitOrius/Kolabore-site
       npm run build
       npx serve out -p 3001
       ```
    2. Open browser at http://localhost:3001
    3. Verify: Header is visible with navigation links (Kolabore / Expertise / Engajamento / Executivos / Contato) and "Converse com a Kolabore" CTA button on the right
    4. Verify: Header background is TRANSPARENT at the top of the page (you can see through it)
    5. Scroll down past the fold — verify: Header background becomes solid dark (`bg-ink` near-black) with a subtle blur effect and bottom border
    6. Scroll back to top — verify: Header returns to transparent
    7. Scroll to the bottom of the page — verify: Footer renders with "Kolabore" brand name, nav links, and copyright line
    8. Confirm there are no console errors in browser DevTools
  </how-to-verify>
  <resume-signal>Type "approved" if Header scroll behavior and Footer render correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
```bash
cd /c/GitOrius/Kolabore-site
# Component files exist
test -f src/components/layout/Header.tsx && echo "Header OK"
test -f src/components/layout/Footer.tsx && echo "Footer OK"
test -f src/components/layout/Container.tsx && echo "Container OK"
test -f src/components/layout/Section.tsx && echo "Section OK"
# Header is 'use client' with scroll logic
grep "'use client'" src/components/layout/Header.tsx && echo "use client OK"
grep "window.scrollY > 60" src/components/layout/Header.tsx && echo "scroll threshold OK"
grep "passive: true" src/components/layout/Header.tsx && echo "passive listener OK"
grep "Converse com a Kolabore" src/components/layout/Header.tsx && echo "CTA text OK"
# Layout wired
grep "<Header" src/app/layout.tsx && echo "Header wired OK"
grep "<Footer" src/app/layout.tsx && echo "Footer wired OK"
# Build
npm run build 2>&1 | grep -E "error|compiled|Build complete" | head -5
```
</verification>

<success_criteria>
1. All 4 layout component files exist in `src/components/layout/`
2. `Header.tsx` has `'use client'`, scroll detection at 60px threshold, passive event listener, 5 nav links, CTA button
3. `Footer.tsx` has brand name, nav links, copyright
4. `Container.tsx` provides `max-w-7xl mx-auto px-6 lg:px-12`
5. `Section.tsx` provides `py-20 lg:py-28` with `id` and `className` props
6. Both Header and Footer wired into `src/app/layout.tsx` inside AnimationProvider
7. `npm run build` exits 0
8. Human verifies: Header transparent-to-solid scroll transition works in browser; Footer renders
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md` with:
- Confirmation all 4 layout components were created
- Header behavior: transparent at top, bg-ink/95 + backdrop-blur after 60px scroll
- Navigation links implemented: Kolabore, Expertise, Engajamento, Executivos, Contato
- CTA button: "Converse com a Kolabore" linking to /contato
- Result of human checkpoint (approved or issues found)
- Build status
</output>
