---
phase: 02-homepage
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - src/components/sections/ChallengesSection.tsx
  - src/components/sections/FormatsSection.tsx
  - src/components/sections/SenioritySection.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - HOME-03
  - HOME-04
  - HOME-05
  - DESIGN-01

must_haves:
  truths:
    - "ChallengesSection renders 6 challenge questions as executive questions (not service labels) on a dark background"
    - "FormatsSection renders all 8 formats from src/data/formats.ts in a card grid"
    - "SenioritySection renders 3 metric proof points in a row on a dark/carbon background"
    - "No section imports or hardcodes colors — all use Tailwind token utilities"
    - "All three sections are wired into src/app/page.tsx after AudienceSection"
    - "Build passes with zero TypeScript errors"
  artifacts:
    - path: "src/components/sections/ChallengesSection.tsx"
      provides: "Challenge questions grid on dark background"
      exports: ["ChallengesSection"]
    - path: "src/components/sections/FormatsSection.tsx"
      provides: "Engagement formats card grid"
      exports: ["FormatsSection"]
    - path: "src/components/sections/SenioritySection.tsx"
      provides: "Seniority metric proof points"
      exports: ["SenioritySection"]
  key_links:
    - from: "src/components/sections/ChallengesSection.tsx"
      to: "src/content/homepage.ts"
      via: "import { challenges } from '@/content/homepage'"
      pattern: "import.*challenges.*from.*content/homepage"
    - from: "src/components/sections/FormatsSection.tsx"
      to: "src/data/formats.ts"
      via: "import { formats } from '@/data/formats'"
      pattern: "import.*formats.*from.*data/formats"
    - from: "src/components/sections/SenioritySection.tsx"
      to: "src/content/homepage.ts"
      via: "import { seniority } from '@/content/homepage'"
      pattern: "import.*seniority.*from.*content/homepage"
    - from: "src/app/page.tsx"
      to: "ChallengesSection, FormatsSection, SenioritySection"
      via: "JSX composition after AudienceSection"
      pattern: "<ChallengesSection|<FormatsSection|<SenioritySection"
---

<objective>
Build ChallengesSection, FormatsSection, and SenioritySection — the three middle trust-building sections — and wire them into page.tsx after AudienceSection.

Purpose: These sections answer "what do they solve?" (Challenges), "how do they work?" (Formats), and "why trust them?" (Seniority). Together they form the orient-and-trust layer of the commercial journey.

Output: 3 new section components wired into page.tsx.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-homepage/02-RESEARCH.md
@.planning/phases/02-homepage/02-02-SUMMARY.md

@src/styles/globals.css
@src/components/layout/Container.tsx
@src/components/layout/Section.tsx
@src/components/ui/SectionHeading.tsx
@src/content/homepage.ts
@src/data/formats.ts

<interfaces>
<!-- SectionHeading API (built in 02-02) -->
Props: eyebrow?, title, subtitle?, align ('left'|'center'), light (boolean), className?
Dark backgrounds: light={true} → text-mist title, text-silver subtitle
Light backgrounds: light={false} → text-ink title, text-slate subtitle

<!-- Section API -->
<Section id={string} className={string}>{children}</Section>
Default padding: py-20 lg:py-28

<!-- formats.ts data structure -->
export interface EngagementFormat { id: string; label: string; description: string; }
export const formats: EngagementFormat[] = [ /* 8 items */ ];

<!-- Motion pattern for staggered grids (from RESEARCH.md) -->
'use client'
import * as m from 'motion/react-m';   // ← MUST be motion/react-m, NOT motion/react
containerVariants = { hidden: {}, visible: { transition: { staggerChildren: 0.08 } } }
itemVariants = { hidden: { opacity: 0, y: 16 }, visible: { opacity: 1, y: 0, transition: { duration: 0.5, ease: 'easeOut' } } }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ChallengesSection and FormatsSection</name>

  <read_first>
    - src/content/homepage.ts — challenges constant (sectionTitle + items array of 6 questions)
    - src/data/formats.ts — formats array (8 items, each with id, label, description)
    - .planning/phases/02-homepage/02-RESEARCH.md — Pattern 2 (staggered children), Anti-Patterns section (CTAs after every section — do NOT add CTAs to these sections per PRD)
    - src/styles/globals.css — token names
  </read_first>

  <files>src/components/sections/ChallengesSection.tsx, src/components/sections/FormatsSection.tsx</files>

  <action>
**src/components/sections/ChallengesSection.tsx**

- `'use client'` directive at top
- Import `m` from `"motion/react-m"`
- Import `{ challenges }` from `"@/content/homepage"`
- Import `{ Section }` from `"@/components/layout/Section"`
- Import `{ Container }` from `"@/components/layout/Container"`
- Import `{ SectionHeading }` from `"@/components/ui/SectionHeading"`

Structure:
```tsx
<Section id="desafios" className="bg-ink">
  <Container>
    <SectionHeading
      title={challenges.sectionTitle}
      align="left"
      light={true}
    />
    <m.div
      variants={containerVariants}
      initial="hidden"
      whileInView="visible"
      viewport={{ once: true, amount: 0.1 }}
      className="grid grid-cols-1 md:grid-cols-2 gap-4"
    >
      {challenges.items.map((question, i) => (
        <m.div
          key={i}
          variants={itemVariants}
          className="border-l-2 border-gold pl-5 py-3"
        >
          <p className="text-mist text-body leading-snug">{question}</p>
        </m.div>
      ))}
    </m.div>
  </Container>
</Section>
```

containerVariants and itemVariants defined as constants above the component function (not inside).

**src/components/sections/FormatsSection.tsx**

- `'use client'` directive at top
- Import `m` from `"motion/react-m"`
- Import `{ formats }` from `"@/data/formats"` — use the data file directly, not the copy file
- Import `{ formats as formatsCopy }` from `"@/content/homepage"` — for sectionTitle and subtitle only
- Import `{ Section }`, `{ Container }`, `{ SectionHeading }`

Structure:
```tsx
<Section id="atuacao" className="bg-mist">
  <Container>
    <SectionHeading
      title={formatsCopy.sectionTitle}
      subtitle={formatsCopy.subtitle}
      align="left"
      light={false}
    />
    <m.div
      variants={containerVariants}
      initial="hidden"
      whileInView="visible"
      viewport={{ once: true, amount: 0.1 }}
      className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4"
    >
      {formats.map((format) => (
        <m.div
          key={format.id}
          variants={itemVariants}
          className="bg-white border border-slate/20 rounded-[var(--radius-card)] p-5"
        >
          <p className="font-sans font-semibold text-ink text-body">{format.label}</p>
          <p className="mt-2 text-small text-slate leading-relaxed">{format.description}</p>
        </m.div>
      ))}
    </m.div>
  </Container>
</Section>
```
  </action>

  <verify>
    <automated>cd C:/GitOrius/Kolabore-site && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - `grep "motion/react-m" src/components/sections/ChallengesSection.tsx` returns a match
    - `grep "motion/react-m" src/components/sections/FormatsSection.tsx` returns a match
    - `grep -rn "motion/react\"" src/components/sections/ChallengesSection.tsx src/components/sections/FormatsSection.tsx` returns 0 lines
    - `grep "border-l-2 border-gold" src/components/sections/ChallengesSection.tsx` returns a match (gold accent on challenge cards)
    - `grep "from.*@/data/formats" src/components/sections/FormatsSection.tsx` returns a match (uses real data file)
    - `grep -rn "#b8933f\|#0d0d0d\|#f5f5f4" src/components/sections/ChallengesSection.tsx src/components/sections/FormatsSection.tsx` returns 0 lines
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>ChallengesSection shows 6 questions with gold left-border accent. FormatsSection shows all 8 format cards from formats.ts. Both compile cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Build SenioritySection and wire all three into page.tsx</name>

  <read_first>
    - src/content/homepage.ts — seniority constant (sectionTitle + metrics array of 3 items, each with value/label/detail)
    - .planning/phases/02-homepage/02-RESEARCH.md — Seniority Section Format notes ("sem exagero visual" — no large counter animations, just clean metric blocks)
    - src/app/page.tsx — current state after 02-02 (has HeroSection and AudienceSection)
  </read_first>

  <files>src/components/sections/SenioritySection.tsx, src/app/page.tsx</files>

  <action>
**src/components/sections/SenioritySection.tsx**

- `'use client'` directive at top
- Import `m` from `"motion/react-m"`
- Import `{ seniority }` from `"@/content/homepage"`
- Import `{ Section }`, `{ Container }`, `{ SectionHeading }`

Structure:
```tsx
<Section id="senioridade" className="bg-carbon">
  <Container>
    <SectionHeading
      title={seniority.sectionTitle}
      align="left"
      light={true}
    />
    <m.div
      variants={containerVariants}
      initial="hidden"
      whileInView="visible"
      viewport={{ once: true, amount: 0.2 }}
      className="grid grid-cols-1 md:grid-cols-3 gap-8 md:gap-12"
    >
      {seniority.metrics.map((metric, i) => (
        <m.div
          key={i}
          variants={itemVariants}
          className="border-t border-slate pt-6"
        >
          <p className="font-display text-h1 text-gold leading-none">{metric.value}</p>
          <p className="mt-2 font-sans font-semibold text-mist text-body">{metric.label}</p>
          <p className="mt-3 text-small text-silver leading-relaxed">{metric.detail}</p>
        </m.div>
      ))}
    </m.div>
  </Container>
</Section>
```

containerVariants and itemVariants defined as constants above the function (same pattern as Task 1).

**src/app/page.tsx**

Add the 3 new sections after AudienceSection. The file currently has HeroSection and AudienceSection. Update to:
```tsx
import { HeroSection } from '@/components/sections/HeroSection';
import { AudienceSection } from '@/components/sections/AudienceSection';
import { ChallengesSection } from '@/components/sections/ChallengesSection';
import { FormatsSection } from '@/components/sections/FormatsSection';
import { SenioritySection } from '@/components/sections/SenioritySection';

export default function HomePage() {
  return (
    <>
      <HeroSection />
      <AudienceSection />
      <ChallengesSection />
      <FormatsSection />
      <SenioritySection />
    </>
  );
}
```
  </action>

  <verify>
    <automated>cd C:/GitOrius/Kolabore-site && npm run build 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `grep "motion/react-m" src/components/sections/SenioritySection.tsx` returns a match
    - `grep "font-display text-h1 text-gold" src/components/sections/SenioritySection.tsx` returns a match (metric value is display text in gold)
    - `grep -c "Section" src/app/page.tsx` returns at least 5 (HeroSection + 4 others imported)
    - `grep "<ChallengesSection" src/app/page.tsx` returns a match
    - `grep "<FormatsSection" src/app/page.tsx` returns a match
    - `grep "<SenioritySection" src/app/page.tsx` returns a match
    - `npm run build` exits 0
  </acceptance_criteria>

  <done>SenioritySection renders 3 metric proof points in carbon-background section. page.tsx composes 5 sections. Build passes.</done>
</task>

</tasks>

<verification>
After plan completion:
- `npm run build && npm run lint` both exit 0
- `grep -rn "motion/react\"" src/components/sections/` returns 0 (all use motion/react-m)
- `grep -rn "#b8933f\|#0d0d0d\|#f5f5f4" src/components/sections/` returns 0
- Manual check: `npm run dev` → verify ChallengesSection shows questions with gold left border, FormatsSection shows 8 format cards, SenioritySection shows 3 metric blocks
</verification>

<success_criteria>
- ChallengesSection: dark background, 6 challenge questions with gold left-border accent, whileInView animation
- FormatsSection: light background, 8 format cards from formats.ts in 4-col desktop grid
- SenioritySection: carbon background, 3 metric proof points with display-size value in gold
- All 5 sections wired in page.tsx
- Build and lint pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-homepage/02-03-SUMMARY.md`
</output>
